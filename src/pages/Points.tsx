import { Loading } from "../components/Loading"

interface PointsProps {
    data: number[]
}

export const Points = ({ data }: PointsProps) => {
    return (
        <div>
            <h1>POINTS PAGE</h1>
        </div>
    )
}